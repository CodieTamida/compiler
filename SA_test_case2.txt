$
    function alert(p1, p2 real, x, y, z boolean) 
        integer sum;
        real p1, p2, p3, p4;
    {
        sum = p1 + p2;
        if (sum != 0)
        {
            p1 = 1;
            p2 = 2;
            p3 = p1 * p2;
            p4 = p3 / 5.5;

            if (p4 => 10)
            {
                success = true;
                return success;
            }
            endif
        }
        endif                    
    }

    function isPrime(n integer) boolean prime; integer i; 
    {
        prime = true;
        i = 2;
        while (i <= n) {
            if (mod(n, i) == 0) 
                prime = false;
            else
                i = i + 1;
            endif                        
        }
        endwhile
        return prime;
    }
$

$ 
    { a= b + z; } 
    while (d == true)
    {
        a= b + z;
        if (a > 0)
            a = a / 2;
        endif
    }
    endwhile
$